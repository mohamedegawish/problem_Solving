#include <bits/stdc++.h>
using namespace std;
#define ll long long
pair<int,int>a[1000];

int main() {
    ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
    ll num;cin>>num;
    ll fib=1,last=0,next;
    for(int i=0;i<num;i++){
            if(i==0){
                cout<<0<<' ';
                continue;
            }
            else if(i==1){
                cout<<1<<' ';
                continue;
            }

            else{
            next=last+fib;
            last=fib;
            fib=next;
            cout<<next<<' ';

            }


    }
}
